# Vite Basic Example

Create a Vite app from your phone:

```sh
cd ~/projects
npm create vite@latest vite-basic
cd vite-basic
npm install
npm run dev -- --host 127.0.0.1
pc open http://127.0.0.1:5173
```
